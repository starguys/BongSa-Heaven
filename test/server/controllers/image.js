module.exports = {
  imageControl: async (req, res) => {
    // const upload = multer({
    //   storage: storage,
    //   limits: { fileSize: 1000000 },
    // })
    return res.send("이미지 잘 와요!");
  },
};
